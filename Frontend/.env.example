# === FRONTEND ENVIRONMENT VARIABLES ===
# For development: Leave VITE_API_BASE_URL empty (or set to http://localhost:5000/api)
# axios.js will auto-detect and use localhost in dev mode
#
# For production (cPanel):
# Set this to your production backend URL, e.g. https://api.yourdomain.com/api
# OR if backend is on same server: https://yourdomain.com/api

VITE_API_BASE_URL=https://api.yourdomain.com/api
